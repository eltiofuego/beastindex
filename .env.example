# BEASTINDEX — no credentials are required to build or run the site.
# Reference data is fetched from public sources with no auth (see data/download_*.py).
# Kaggle is NOT used, so no kaggle.json is needed.

# Needed only once the submissions database (revision doc POIN 2) is built:
# NEXT_PUBLIC_SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
